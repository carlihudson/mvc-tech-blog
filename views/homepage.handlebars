{{#each posts as |post|}}
<div class="card">
  <div class="card-header">
   {{post.title}}
    <br>
    - {{user.username}}
  </div>
  <div class="card-body">
    <p class="card-text">{{post.post_content}}.</p>
    <a href="#" class="btn btn-primary">Comments</a>
  </div>
</div>
{{/each}}