{{#each posts as |post|}}
  <div class='row mb-4 items'>
    <div class='col-md-5'>
      <h2>
        <!-- Render item names and details -->
        <h3>{{post.title}}</h3>
      </h2>
        <p>{{post.post_content}}</p>
      </ul>
    </div>
  </div>
{{/each}}