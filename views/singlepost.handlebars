<h3>{{title}}</h3>
<h4> - {{user.username}} at {{timestamp}} </h4>
    <div class="card">
  <div class="card-body">
    <p class="card-text">{{post_content}}.</p>
    </card>
   {{#each comments as |comment|}}
<div class="card">
  <div class="card-body">
    <p class="card-text">{{comment.comment_content}}.</p>
  </div>
</div>
{{/each}}
<form>
   <div class="form-group">
    {{!-- <label for="write-new-post">Write a New Post</label> --}}
    <textarea class="form-control" id="new-comment" placeholder="Write a Comment" rows="3"></textarea>
  </div>
</form>
    <a href="#" class="btn btn-primary" id="add-comment">Add a Comment</a>
  </div>
</div>


<script src="../js/singlepost.js"></script>
<script src="../js/addcomment.js"></script>